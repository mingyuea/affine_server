DROP TABLE IF EXISTS userinfo;

CREATE TABLE userinfo (
	fname VARCHAR(40),
	mname VARCHAR(40),
	lname VARCHAR(50),
	address VARCHAR(300),
	city VARCHAR(150),
	state VARCHAR(30),
	country VARCHAR(40)
);